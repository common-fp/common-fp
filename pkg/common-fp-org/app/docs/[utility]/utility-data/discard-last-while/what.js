import code from '@/built/code-examples/api/discard-last-while'

const what = {
  example: code.minimal,
  Description: () => <p>Discard last elements that pass a condition</p>,
}

export default what
