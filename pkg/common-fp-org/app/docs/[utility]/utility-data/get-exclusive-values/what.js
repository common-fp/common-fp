import code from '@/built/code-examples/api/get-exclusive-values'

const what = {
  example: code.minimal,
  Description: () => <p>Return the values exclusively in one collection</p>,
}

export default what
