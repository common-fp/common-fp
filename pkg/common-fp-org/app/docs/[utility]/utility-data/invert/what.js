import { KeyedCollection } from '@/cmpt/inline-types'
import code from '@/built/code-examples/api/invert'

const what = {
  example: code.minimal,
  Description: () => (
    <p>
      Swap the values and keys of a <KeyedCollection />
    </p>
  ),
}

export default what
