import code from '@/built/code-examples/api/number-is-between'

const what = {
  example: code.minimal,
  Description: () => (
    <p>Test if a number is between two others. Inclusive by default.</p>
  ),
}

export default what
