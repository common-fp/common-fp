import code from '@/built/code-examples/api/get-keys'

const what = {
  example: code.minimal,
  Description: () => {
    return (
      <>
        <p>{`Return an array of the collection's keys`}</p>
      </>
    )
  },
}

export default what
