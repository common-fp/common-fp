import code from '@/built/code-examples/api/discard-first-while'

const what = {
  example: code.minimal,
  Description: () => <p>Discard the first elements that pass a condition</p>,
}

export default what
