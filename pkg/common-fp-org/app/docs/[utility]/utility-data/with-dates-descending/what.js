import code from '@/built/code-examples/api/with-dates-descending'
import CompareFunction from '@/docs/[utility]/cmpt/compare-function'

const what = {
  example: code.minimal,
  Description: () => (
    <p>
      A <CompareFunction /> which orders dates from latest to earliest.
    </p>
  ),
}

export default what
