const Notes = () => (
  <>
    <p>
      {`
      When resolving the path, this utility does not treat Maps differently.  It
      only gets object properties.
      `}
    </p>
  </>
)

export default Notes
