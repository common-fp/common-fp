import code from '@/built/code-examples/api/is-at-or-after'

const what = {
  example: code.minimal,
  Description: () => <p>Test if a date is the same as or after another</p>,
}

export default what
