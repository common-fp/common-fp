import code from '@/built/code-examples/api/p-all'

const what = {
  example: code.minimal,
  Description: () => (
    <p>One at a time, asynchronously test if all entries pass a condition</p>
  ),
}

export default what
