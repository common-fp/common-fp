import code from '@/built/code-examples/api/is-at-or-before'

const what = {
  example: code.minimal,
  Description: () => <p>Test if a date is the same as or before another</p>,
}

export default what
