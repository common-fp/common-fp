import code from '@/built/code-examples/api/with-dates-ascending'
import CompareFunction from '@/docs/[utility]/cmpt/compare-function'

const what = {
  example: code.minimal,
  Description: () => (
    <p>
      A <CompareFunction /> which orders dates from earliest to latest.
    </p>
  ),
}

export default what
