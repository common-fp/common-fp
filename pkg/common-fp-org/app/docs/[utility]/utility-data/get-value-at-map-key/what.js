import code from '@/built/code-examples/api/get-value-at-map-key'

const what = {
  example: code.minimal,
  Description: () => <p>{`Return the value found at the map's key`}</p>,
}

export default what
