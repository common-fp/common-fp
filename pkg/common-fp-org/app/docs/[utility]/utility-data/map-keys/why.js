const Why = () => (
  <>
    <p>{`See 'What' for example code.`}</p>
    <p>
      {`
      One use case is when we have a collection with multiple unique properties.
      Different parts of the application may have access to one of these
      properties, but not all.  In these situations, it's helpful to map the
      collection by the available key.
      `}
    </p>
  </>
)

export default Why
