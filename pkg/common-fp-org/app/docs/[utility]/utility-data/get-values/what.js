import code from '@/built/code-examples/api/get-values'

const what = {
  example: code.minimal,
  Description: () => {
    return (
      <>
        <p>{`Return an array of the collection's values`}</p>
      </>
    )
  },
}

export default what
