import code from '@/built/code-examples/api/date-is-between'

const what = {
  example: code.minimal,
  Description: () => (
    <p>Test if a date is between two others. Inclusive by default.</p>
  ),
}

export default what
