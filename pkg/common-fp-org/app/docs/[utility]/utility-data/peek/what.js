import code from '@/built/code-examples/api/peek'

const what = {
  example: code.minimal,
  Description: () => (
    <>
      <p>Peek at a value in a function chain without modifying it.</p>
    </>
  ),
}

export default what
