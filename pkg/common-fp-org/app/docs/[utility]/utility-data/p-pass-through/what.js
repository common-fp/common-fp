import code from '@/built/code-examples/api/p-pass-through'
import ComposeNote from '@/docs/[utility]/cmpt/compose-note'

const what = {
  example: code.minimal,
  Description: () => (
    <>
      <p>Pass a value through asynchronous functions, returning the result.</p>
      <ComposeNote />
    </>
  ),
}

export default what
