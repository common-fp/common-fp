import code from '@/built/code-examples/api/p-any'

const what = {
  example: code.minimal,
  Description: () => (
    <p>One at a time, asynchronously test if any entry passes a condition</p>
  ),
}

export default what
