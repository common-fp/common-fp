import code from '@/built/code-examples/api/get-max-value'

const what = {
  example: code.minimal,
  Description: () => (
    <>
      <p>Return the largest number in the collection</p>
    </>
  ),
}

export default what
