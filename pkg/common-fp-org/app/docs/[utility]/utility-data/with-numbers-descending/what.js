import code from '@/built/code-examples/api/with-numbers-descending'
import CompareFunction from '@/docs/[utility]/cmpt/compare-function'

const what = {
  example: code.minimal,
  Description: () => (
    <p>
      A <CompareFunction /> which orders numbers from largest to smallest.
    </p>
  ),
}

export default what
