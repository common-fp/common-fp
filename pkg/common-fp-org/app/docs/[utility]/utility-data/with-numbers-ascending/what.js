import code from '@/built/code-examples/api/with-numbers-ascending'
import CompareFunction from '@/docs/[utility]/cmpt/compare-function'

const what = {
  example: code.minimal,
  Description: () => (
    <p>
      A <CompareFunction /> which orders numbers from smallest to largest.
    </p>
  ),
}

export default what
