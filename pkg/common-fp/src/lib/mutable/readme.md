**note**: typescript doesn't make a lot of sense with mutable parameters because
variable types cannot be mutated. Therefore these utilities return input
types, expecting devs to pass them correctly.
